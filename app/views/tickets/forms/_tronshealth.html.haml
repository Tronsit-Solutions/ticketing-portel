%div{class: fld}
  %label{class: lbl} Issue
  = select_tag "ticket[metadata][issue]", options_for_select(DropdownOption.for(DropdownOption::TRONSHEALTH_ISSUE).pluck(:label, :value), ticket.metadata&.dig("issue")), class: sel, prompt: "Select an issue category"
